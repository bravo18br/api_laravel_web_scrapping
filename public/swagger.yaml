openapi: 3.0.0
info:
  title: Monitora Sites API
  description: https://github.com/bravo18br/api_laravel_web_scrapping
  version: 1.0.0
tags:
  - name: Usuários
    description: Operações relacionadas a usuários
  - name: WhatsApp
    description: Operações relacionadas ao WhatsApp

paths:
  /users:
    get:
      tags:
        - Usuários
      summary: Lista todos os usuários
      responses:
        '200':
          description: Uma lista de usuários
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/User'

  /api/wpp/getQRCodeBIN:
    get:
      tags:
        - WhatsApp
      summary: Obtém o QR Code em formato BIN
      responses:
        '200':
          description: Retorna QR Code em formato BIN em caso de sucesso
          content:
            application/json:
              schema:
                type: object
                properties:
                  qrcode:
                    type: string
                    example: "data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAA..."
                  status:
                    type: string
                    example: 'Sucesso'
        '500':
          description: Retorna mensagem de erro em caso de falha
          content:
            application/json:
              schema:
                type: object
                properties:
                  qrcode:
                    type: string
                    example: "Erro function geraQRCodeWPP..."
                  status:
                    type: string
                    example: 'Falha'
  /api/wpp/getQRCodePNG:
    get:
      tags:
        - WhatsApp
      summary: Obtém o QR Code em formato PNG para download
      responses:
        '200':
          description: QR Code em formato PNG para download
          content:
            image/png:
              schema:
                type: Deve iniciar o download de um qrcode.png
                format: PNG

  /api/wpp/getStatusWPP:
    get:
      tags:
        - WhatsApp
      summary: Obtém o status do WhatsApp
      responses:
        '200':
          description: Status do WhatsApp
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    example: CONNECTED
                  ERRO:
                    type: string
                    example: "Erro function statusWPP: 500"

components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: integer
        name:
          type: string
        email:
          type: string
