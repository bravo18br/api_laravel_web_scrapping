<!DOCTYPE html>
<html>
<head>
    <title>{{ $titulo }}</title>
</head>
<body>
    <h1>{{ $titulo }}</h1>
    <p>{{ $mensagem }}</p>
    <img src="data:image/png;base64,{{ $qrcode }}" alt="QR Code">
</body>
</html>
