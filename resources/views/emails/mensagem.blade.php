<!DOCTYPE html>
<html>
<head>
    <title>{{ $titulo }}</title>
</head>
<body>
    <h1>Notificação: </h1>
    <p>{{ $mensagem }}</p>
    <h1>WPPConnect Status: </h1>
    <!-- <img src="data:image/png;base64,{{ $qrcode }}" alt="QR Code"> -->
    <!-- <img src="{{ $message->embed(public_path('storage/qrcode.png')) }}" alt="QR Code"> -->
</body>
</html>
